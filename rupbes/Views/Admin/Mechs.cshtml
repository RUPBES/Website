
@{
    ViewBag.Title = "Техника";
}


<section class="admin-content">
    @Html.ActionLink("Назад", "Index")
    <h2>Тахника филиалов</h2>
    @foreach (rupbes.Models.Departments department in Model)
    {
        <p>@Html.ActionLink(department.name_ru, "DepMechs", "Admin", new { id = department.id }, null)</p>
    }
</section>

