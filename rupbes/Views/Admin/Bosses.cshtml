
@{
    ViewBag.Title = "Руководство";
}


<section class="admin-content">
    @Html.ActionLink("Назад", "Index")
    <h2>Руководство филиалов</h2>
    @foreach (rupbes.Models.Departments department in Model)
    {
        <p>@Html.ActionLink(department.name_ru, "DepBosses", "Admin", new { id = department.id }, null)</p>
    }
</section>

