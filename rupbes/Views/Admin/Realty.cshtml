
@{
    ViewBag.Title = "Аренда";
}


<section class="admin-content">
    @Html.ActionLink("Назад", "Index")
    <h2>Аренда по филиалам</h2>
    @foreach (rupbes.Models.Departments department in Model)
    {
        <p>@Html.ActionLink(department.name_ru, "DepRealty", "Admin", new { id = department.id }, null)</p>
    }
</section>