@{
    ViewBag.Title = "Продажа имущества";
}


<section class="admin-content">
    @Html.ActionLink("Назад", "Index")
    <h2>Продажа имущества по филиалам</h2>
    @foreach (rupbes.Models.Departments department in Model)
    {
        <p>@Html.ActionLink(department.name_ru, "DepSale", "Admin", new { id = department.id }, null)</p>
    }
</section>